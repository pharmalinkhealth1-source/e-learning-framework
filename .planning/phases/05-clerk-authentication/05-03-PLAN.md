---
wave: 3
depends_on: [05-02]
files_modified: [src/app/(auth)/onboarding/page.tsx, src/app/api/auth/onboarding/route.ts]
requirements_addressed: [PHASE_05_ROLES]
autonomous: true
---

# Plan 05-03: Role Management & Onboarding

Implement post-signup onboarding to collect user roles and profile data.

## Objectives
- Create `/onboarding` page to collect "Company Name", "Use Case", and "Role".
- Save data to Clerk `publicMetadata`.
- Redirect users to onboarding if metadata is missing.

## Tasks
<task>
<action>
Create `src/app/(auth)/onboarding/page.tsx`.
Form should include:
- Role selection (Client, Developer, Partner) using custom segmented controls.
- "Company Name" text input.
- "Primary Use Case" text input.
</action>
<read_first>
- src/app/(auth)/sign-up/[[...sign-up]]/page.tsx
</read_first>
<acceptance_criteria>
- `/onboarding` renders with 3 specific role options.
- Inputs follow HDS design tokens.
</acceptance_criteria>
</task>

<task>
<action>
Create `src/app/api/auth/onboarding/route.ts` to handle metadata updates.
Use `clerkClient.users.updateUserMetadata` to save choices.
</action>
<read_first>
- src/sanity/lib/client.ts
</read_first>
<acceptance_criteria>
- API endpoint handles POST requests.
- Successfully updates `publicMetadata` in Clerk.
</acceptance_criteria>
</task>

<task>
<action>
Update middleware or layout to redirect authenticated users without roles to `/onboarding`.
</action>
<read_first>
- src/middleware.ts
</read_first>
<acceptance_criteria>
- User is redirected to `/onboarding` if `publicMetadata.role` is missing.
</acceptance_criteria>
</task>

## Verification
- Signup a new user.
- Verify they are immediately redirected to `/onboarding`.
- Submit onboarding and verify metadata is present in Clerk.
