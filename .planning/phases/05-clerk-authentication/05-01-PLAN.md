---
wave: 1
depends_on: []
files_modified: [package.json, src/middleware.ts, .env.local]
requirements_addressed: [PHASE_05_AUTH]
autonomous: true
---

# Plan 05-01: Setup & Middleware

Install Clerk dependencies and configure the authentication boundary.

## Objectives
- Install `@clerk/nextjs` and `svix`.
- Configure environment variables.
- Implement Next.js Middleware for route protection.

## Tasks
<task>
<action>
Install dependencies: `npm install @clerk/nextjs svix`.
</action>
<read_first>
- package.json
</read_first>
<acceptance_criteria>
- `package.json` contains `@clerk/nextjs` and `svix`.
</acceptance_criteria>
</task>

<task>
<action>
Create `src/middleware.ts` with Clerk middleware.
Protect all routes under `/forum`.
</action>
<read_first>
- src/app/layout.tsx
</read_first>
<acceptance_criteria>
- `src/middleware.ts` exists.
- Contains `clerkMiddleware` and `createRouteMatcher(['/forum(.*)'])`.
</acceptance_criteria>
</task>

<task>
<action>
Add Clerk environment variable placeholders to `.env.example`.
</action>
<read_first>
- .env.example
</read_first>
<acceptance_criteria>
- `.env.example` contains `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` and `CLERK_SECRET_KEY`.
</acceptance_criteria>
</task>

## Verification
- Run `npm run dev` and attempt to visit `/forum`. Should redirect to Clerk's default or custom sign-in.
