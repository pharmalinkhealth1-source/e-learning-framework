---
wave: 4
depends_on: [05-03]
files_modified: [src/app/api/webhooks/clerk/route.ts]
requirements_addressed: [PHASE_05_SANITY_SYNC]
autonomous: true
---

# Plan 05-04: Sanity Sync (Webhook)

Keep Sanity "Author" records in sync with Clerk users for the forum.

## Objectives
- Create a webhook handler for Clerk `user.created` and `user.updated`.
- Verify webhook signatures using `svix`.
- Upsert `author` documents in Sanity with Clerk `userId`.

## Tasks
<task>
<action>
Create `src/app/api/webhooks/clerk/route.ts`.
Implement `svix` signature verification.
</action>
<read_first>
- src/sanity/lib/client.ts
</read_first>
<acceptance_criteria>
- Endpoint responds to Clerk webhooks.
- Signature verification is active.
</acceptance_criteria>
</task>

<task>
<action>
Implement upsert logic for Sanity `author` documents.
Map Clerk `firstName`, `lastName`, and `imageUrl` to Sanity schema.
Store Clerk `userId` as a hidden field in Sanity for linking.
</action>
<read_first>
- src/sanity/schemaTypes/forumPost.ts
</read_first>
<acceptance_criteria>
- `author` documents are created/updated in Sanity on Clerk events.
</acceptance_criteria>
</task>

## Verification
- Use Clerk dashboard to "Test Webhook" with a sample payload.
- Verify a matching document appears in Sanity Studio at `/studio`.
