---
wave: 2
depends_on: [05-01]
files_modified: [src/app/(auth)/sign-in/[[...sign-in]]/page.tsx, src/app/(auth)/sign-up/[[...sign-up]]/page.tsx, src/components/stripe/AuthLayout.tsx]
requirements_addressed: [PHASE_05_AUTH_UX]
autonomous: true
---

# Plan 05-02: Custom Auth Pages (Fidelity)

Build high-fidelity, Stripe-branded auth pages using Clerk Hooks.

## Objectives
- Create a shared `AuthLayout` with `MeshGradient`.
- Implement `/sign-in` and `/sign-up` pages with custom forms.
- Ensure 100% fidelity to Stripe typography and spacing.

## Tasks
<task>
<action>
Create `src/components/stripe/AuthLayout.tsx`.
Wrap children with `MeshGradient` and a centered glassmorphism card.
</action>
<read_first>
- src/components/stripe/MeshGradient.tsx
- src/components/stripe/Bento.tsx
</read_first>
<acceptance_criteria>
- `AuthLayout.tsx` renders `MeshGradient`.
- Uses HDS background blur and shadow tokens.
</acceptance_criteria>
</task>

<task>
<action>
Implement `src/app/(auth)/sign-in/[[...sign-in]]/page.tsx` using `useSignIn` from `@clerk/nextjs`.
Create a custom form with email and password inputs matching Stripe's "Clinical Luxury" style.
</action>
<read_first>
- src/styles/tokens.css
- src/components/stripe/Hero.tsx
</read_first>
<acceptance_criteria>
- Page renders at `/sign-in`.
- Form uses `Inter` font and high-fidelity input borders.
</acceptance_criteria>
</task>

<task>
<action>
Implement `src/app/(auth)/sign-up/[[...sign-up]]/page.tsx` using `useSignUp`.
Ensure same styling as sign-in.
</action>
<read_first>
- src/app/(auth)/sign-in/[[...sign-in]]/page.tsx
</read_first>
<acceptance_criteria>
- Page renders at `/sign-up`.
</acceptance_criteria>
</task>

## Verification
- Visual inspection: `/sign-in` and `/sign-up` must look like genuine Stripe pages.
- Functional check: Forms should submit to Clerk and handle errors (e.g. invalid credentials) with branded error states.
